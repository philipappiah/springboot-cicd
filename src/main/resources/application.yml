server:
  port: 9000
spring:
  profiles:
    active: local

---
spring:
  config:
    activate:
      on-profile: local
  data:
    mongodb:
      database: ProductsDB
      host: localhost
      port: 27017

